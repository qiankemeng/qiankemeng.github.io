# Netlify Identity 用户邀请指南

## 📋 完整步骤

### 步骤 1: 登录 Netlify 并进入站点

1. **访问 Netlify**：https://app.netlify.com/
2. **登录**：使用您的 GitHub 账号登录
3. **选择站点**：在站点列表中找到并点击您的站点名称
   - 站点名称通常是随机生成的，如 `amazing-nightingale-123456`
   - 或者您自定义的域名

---

### 步骤 2: 确认 Identity 已启用

1. 在站点控制台，点击顶部导航栏的 **"Site configuration"** 或 **"Settings"**

2. 在左侧菜单找到并点击 **"Identity"**

3. 检查状态：
   - ✅ 如果显示 **"Identity enabled"**，说明已启用，继续下一步
   - ❌ 如果显示 **"Enable Identity"** 按钮，点击它启用

4. 确认 Git Gateway 已启用：
   - 向下滚动到 **"Services"** 部分
   - 找到 **"Git Gateway"**
   - ✅ 如果显示 "Enabled"，说明已启用
   - ❌ 如果显示 "Enable Git Gateway" 按钮，点击它启用

---

### 步骤 3: 配置 Identity 设置（重要）

在启用 Identity 后，需要配置注册方式：

1. 在 **Identity** 页面，找到 **"Registration preferences"** 或 **"Registration"**

2. 选择注册方式：
   - **推荐选择：Invite only**（仅邀请）
     - 只有被邀请的用户才能访问
     - 更安全，适合个人博客

   - 或选择：Open（开放注册）
     - 任何人都可以注册
     - ⚠️ 不推荐，除非您想公开开放

3. 点击 **"Save"** 保存设置

---

### 步骤 4: 邀请用户（自己）

现在可以邀请用户了：

#### 方法 A：通过 Identity 标签页（推荐）

1. 在站点控制台顶部，点击 **"Identity"** 标签
   - 注意：这是顶部的标签页，不是左侧菜单的 Settings > Identity

2. 点击 **"Invite users"** 按钮

3. 在弹出的对话框中：
   - **输入邮箱**：输入您要使用的邮箱地址（可以是任何邮箱）
   - 点击 **"Send"** 或 **"Invite"**

4. 完成！您会看到：
   - 用户出现在 Identity 列表中
   - 状态显示为 **"Invited"**

#### 方法 B：通过 Settings（替代方法）

1. **Settings** → **Identity** → 向下滚动找到 **"Invite users"** 按钮
2. 输入邮箱地址
3. 点击发送邀请

---

### 步骤 5: 接受邀请并设置密码

1. **查收邮件**：
   - 检查您刚才输入的邮箱
   - 标题类似："You've been invited to join ..."
   - 发件人：Netlify Identity

2. **点击邀请链接**：
   - 打开邮件，点击 **"Accept the invite"** 按钮
   - 或点击邮件中的链接

3. **设置密码**：
   - 浏览器会打开设置密码页面
   - 输入新密码（至少6个字符）
   - 确认密码
   - 点击 **"Sign up"** 或 **"Set password"**

4. **确认成功**：
   - 您应该看到确认消息："Your account has been created"
   - 返回 Netlify 站点的 Identity 标签页
   - 用户状态应该从 "Invited" 变为 **"Active"**

---

### 步骤 6: 测试登录 CMS

现在可以测试登录了：

1. **访问 CMS 管理后台**：
   ```
   https://qiankemeng.github.io/admin/
   ```

2. **点击登录**：
   - 应该看到 **"Login with Netlify Identity"** 按钮
   - 点击它

3. **输入凭据**：
   - **Email**：您刚才邀请的邮箱
   - **Password**：您设置的密码
   - 点击 **"Log in"**

4. **成功！**：
   - 您应该进入 CMS 管理界面
   - 可以看到所有博客分类
   - 可以创建和编辑文章了

---

## 🔍 故障排查

### 问题 1: 没有收到邀请邮件

**可能原因和解决方案：**

1. **邮件在垃圾箱**：
   - 检查垃圾邮件/促销邮件文件夹
   - 搜索发件人 "Netlify" 或 "noreply@netlify.com"

2. **邮箱地址输入错误**：
   - 在 Netlify Identity 标签页删除错误的邀请
   - 重新邀请，仔细检查邮箱拼写

3. **邮件发送延迟**：
   - 等待 5-10 分钟
   - 如果还没收到，在 Netlify 重新发送邀请

4. **手动重发邀请**：
   - 在 Identity 标签页找到用户
   - 点击用户旁边的 **"..."** 或 **"Resend invite"**

### 问题 2: 找不到 "Invite users" 按钮

**解决方案：**

1. **确认 Identity 已启用**：
   - Settings → Identity → 应该显示 "Enabled"
   - 如果没有，点击 "Enable Identity"

2. **刷新页面**：
   - 启用 Identity 后，刷新浏览器
   - "Invite users" 按钮应该出现

3. **切换到正确的标签页**：
   - 顶部有两个位置：
     - **Identity 标签页**（顶部导航）← 在这里邀请
     - Settings → Identity（左侧菜单）← 这里是配置

### 问题 3: 邀请链接过期

**解决方案：**

1. 在 Netlify Identity 标签页找到用户
2. 点击用户旁边的 **"..."** 菜单
3. 选择 **"Resend invite"**
4. 查收新邮件并及时完成注册

### 问题 4: CMS 登录后显示 "Error loading user"

**解决方案：**

1. **确认 Git Gateway 已启用**：
   - Settings → Identity → Services → Git Gateway
   - 应该显示 "Enabled"

2. **检查仓库权限**：
   - GitHub 仓库 → Settings → Actions → General
   - Workflow permissions 应该设置为 "Read and write permissions"

3. **清除浏览器缓存**：
   - 按 Ctrl+Shift+Delete（Windows）或 Cmd+Shift+Delete（Mac）
   - 清除缓存和 Cookies
   - 重新登录

---

## 📸 关键界面说明

### Netlify 站点控制台结构

```
顶部导航栏：
├── Site overview（站点概览）
├── Deploys（部署）
├── Identity（← 在这里邀请用户）
├── Logs（日志）
└── ...

左侧菜单（Settings）：
├── General
├── Domain management
├── Identity（← 在这里配置设置）
│   ├── Enable Identity 按钮
│   ├── Registration preferences
│   ├── External providers
│   └── Services
│       └── Git Gateway
└── ...
```

### Identity 标签页布局

```
┌─────────────────────────────────────┐
│  Identity                            │
├─────────────────────────────────────┤
│  [Invite users] 按钮  [Settings]    │
├─────────────────────────────────────┤
│  用户列表：                          │
│  ┌───────────────────────────────┐  │
│  │ user@example.com  [Active]    │  │
│  │ [...]  ← 点击查看更多选项     │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

---

## ✅ 验证清单

完成邀请后，请确认：

- [ ] Netlify Identity 显示 "Enabled"
- [ ] Git Gateway 显示 "Enabled"
- [ ] 注册方式设置为 "Invite only"
- [ ] 已发送邀请邮件
- [ ] 收到并打开邀请邮件
- [ ] 成功设置密码
- [ ] 用户状态显示为 "Active"
- [ ] 能够访问 `/admin/` 并看到登录按钮
- [ ] 能够成功登录 CMS
- [ ] 能够查看和创建文章

---

## 🎯 快速参考

### 邀请用户的最快路径

```
1. app.netlify.com → 登录
2. 选择站点
3. 顶部点击 "Identity" 标签
4. 点击 "Invite users"
5. 输入邮箱 → 发送
6. 查收邮件 → 点击链接 → 设置密码
7. 完成！
```

### 关键 URL

- **Netlify 控制台**：https://app.netlify.com/
- **您的站点**：https://app.netlify.com/sites/[your-site-name]/
- **Identity 管理**：https://app.netlify.com/sites/[your-site-name]/identity
- **您的 CMS**：https://qiankemeng.github.io/admin/

---

## 📞 需要帮助？

如果按照上述步骤仍然无法邀请用户：

1. **检查 Netlify 状态**：https://www.netlifystatus.com/
2. **Netlify 文档**：https://docs.netlify.com/visitor-access/identity/
3. **Netlify 支持**：https://answers.netlify.com/
4. **查看浏览器控制台**（F12）是否有错误信息

---

## 💡 提示

- **多个用户**：您可以邀请多个用户（团队协作）
- **删除用户**：在 Identity 标签页，点击用户旁的 "..." → "Delete user"
- **重置密码**：用户可以在登录页面点击 "Forgot password" 重置
- **外部登录**：可以配置 GitHub、Google、GitLab 等外部登录（Settings → Identity → External providers）

---

**提示**：首次配置可能需要 5-10 分钟时间，请耐心按照步骤操作。配置一次后，以后就可以直接使用了！
