# Decap CMS - 本地开发配置
# 用于本地测试，无需 OAuth 认证

backend:
  name: git-gateway
  branch: main

# 启用本地后端
local_backend: true

# 媒体文件配置
media_folder: "public/images/blog"
public_folder: "/images/blog"

# 集合配置 - 完整版本
collections:
  # 每日论文 (中文)
  - name: "daily-papers-zh"
    label: "每日论文 (中文)"
    label_singular: "论文"
    folder: "src/content/blog/posts/daily-papers"
    create: true
    slug: "{{arxivId}}-{{year}}-{{month}}-{{day}}"
    extension: "zh.md"
    format: "frontmatter"
    fields:
      - {label: "标题", name: "title", widget: "string"}
      - {label: "发布日期", name: "date", widget: "datetime", format: "YYYY-MM-DD"}
      - {label: "摘要", name: "summary", widget: "text", required: false}
      - {label: "arXiv ID", name: "arxivId", widget: "string"}
      - {label: "作者", name: "authors", widget: "list"}
      - {label: "论文链接", name: "arxivUrl", widget: "string"}
      - {label: "标签", name: "tags", widget: "list", required: false}
      - {label: "内容", name: "body", widget: "markdown"}

  # 学习笔记 (中文)
  - name: "notes-zh"
    label: "学习笔记 (中文)"
    folder: "src/content/blog/posts/notes"
    create: true
    slug: "{{slug}}"
    extension: "zh.md"
    format: "frontmatter"
    fields:
      - {label: "标题", name: "title", widget: "string"}
      - {label: "发布日期", name: "date", widget: "datetime", format: "YYYY-MM-DD"}
      - {label: "摘要", name: "summary", widget: "text", required: false}
      - {label: "标签", name: "tags", widget: "list", required: false}
      - {label: "内容", name: "body", widget: "markdown"}
